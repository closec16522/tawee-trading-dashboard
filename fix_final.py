import re

with open('index.html', 'r', encoding='utf-8') as f:
    content = f.read()

# Remove the one at 8161
content = re.sub(r'const meetingLog = document\.getElementById\(\'ai-meeting-log\'\);\s*if \(meetingLog\) \{\s*function translateCasual.*?meetingTxt\};\s*\}', '', content, flags=re.DOTALL)

# Now, at 8690, find the AGENT_UPDATE block and carefully inject the function
# Let's find:
# if (meetingLog) {
#    const msgDiv = document.createElement('div');
#    msgDiv.className = 'ai-msg';
#    msgDiv.style.borderLeftColor = data.color || "#f59e0b";
#    msgDiv.innerHTML = <strong style="color:"></strong>: ;

pattern_target = r'if \(meetingLog\) \{\s*const msgDiv = document\.createElement\(\'div\'\);\s*msgDiv\.className = \'ai-msg\';\s*msgDiv\.style\.borderLeftColor = data\.color \|\| "#f59e0b";\s*msgDiv\.innerHTML = \<strong style="color:\$\{data\.color \|\| "#f59e0b"\}">\$\{data\.agent_id\.toUpperCase\(\)\}\<\/strong\>: \$\{meetingTxt\};'

clean_block = """if (meetingLog) {
                       function translateCasual(agentId, text) {
                           let t = text.toLowerCase();
                           let r = text;
                           let id = agentId.toLowerCase();
                           if (t.includes("scanning") && t.includes("structure")) {
                               const pair = text.match(/Scanning (.*?) Structure/i);
                               const p = pair ? pair[1] : "????";
                               r = ????? ????????  ?????? ??????????????????? ??;
                           } else if (t.includes("waiting for market data")) {
                               r = ????????????????????????????... ???????????????????????? ??;
                           } else if (t.includes("bullish")) {
                               const pair = text.match(/\[(.*?)\]/);
                               const p = pair ? pair[1] : "";
                               r = ${p} ?????????????????? ?? ?????????????????!;
                           } else if (t.includes("bearish")) {
                               const pair = text.match(/\[(.*?)\]/);
                               const p = pair ? pair[1] : "";
                               r = ${p} ?????????? ??????????????????! ????;
                           } else if (t.includes("evaluating")) {
                               const pair = text.match(/Evaluating (.*?)\\.\\.\\./i);
                               const p = pair ? pair[1] : "????";
                               r = ?????????  ??????????? ?????? ????????????? ??;
                           } else if (t.includes("?????????????????????")) {
                               r = text.replace("???????????????????????????????????????", "?????????????? ?????! ???????")
                                       .replace("???", "??????") + " ??????????????? ??";
                           } else if (t.includes("trade rejected") || t.includes("volatility")) {
                               r = ?????? ?????????????????? ????????????????? ??????????????? ??;
                           } else if (t.includes("calculating risk")) {
                               r = ?????????????????????????????????????????? ????;
                           } else if (t.includes("executing")) {
                               r = ?????????????????????! ??????????????????????????? ????;
                           } else if (t.includes("monitoring")) {
                               r = ????????????????????????????????????????? ??????;
                           } else if (t.includes("?????????????????")) {
                               r = text.replace("?????????????????", "??????? ???????????????????????? ???")
                                       .replace("???????????????????", "?????????????") + " ????????????? ???????! ??";
                           } else if (t.includes("????? ??????????")) {
                               r = text.replace("????? ??????????", "???? ????????");
                           } else {
                               if (id === 'market_analyst') r = text + ' ????????????? ??????';
                               else if (id === 'smc_strategist') r = text + ' ??????????????????? ??';
                               else if (id === 'risk_manager') r = text + ' ?????? ????????? ???';
                               else if (id === 'trade_executor') r = text + ' ????????????! ?';
                               else r = text;
                           }
                           return r;
                       }
                       
                       let meetingTxt = translateCasual(data.agent_id, txt);

                       const msgDiv = document.createElement('div');
                       msgDiv.className = 'ai-msg';
                       msgDiv.style.borderLeftColor = data.color || "#f59e0b";
                       msgDiv.innerHTML = <strong style="color:"></strong>: ;"""

content, count = re.subn(pattern_target, clean_block, content)
print(f"Replaced {count} occurrences in AGENT_UPDATE.")

with open('index.html', 'w', encoding='utf-8') as f:
    f.write(content)
